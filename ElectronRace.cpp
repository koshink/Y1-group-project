#include "ElectronRace.h"

ElectronRace::ElectronRace(TextLCD &lcd) : lcd(lcd) {
    // Initialise game start
}

void ElectronRace::startGame() {
    while (true {
        
        thread_sleep_for(100);
    })
}